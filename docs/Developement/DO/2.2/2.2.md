93.[FAIT-IA] [DEP VERSION] Les trois defauts releves par l'inventaire
       Suite du point 80 (24/09) : x/crypto en trois versions, go-ldap
       importee nulle part, YAML v2 et v3 dans le meme produit.

    -0 golang.org/x/crypto : v0.54.0 partout (vaultaire_ctl venait de v0.47.0,
       vaultaire_serveur de v0.52.0). golang.org/x/sys suit : v0.47.0 partout,
       y compris le socle Ducky et l'agent Windows qui la tirent directement.
    -1 go-ldap/ldap/v3 retiree du core par go mod tidy, avec go-ntlmssp et
       google/uuid. x/crypto est RESTEE directe (argon2id) : verifie.
    -2 Trouve en chemin : l'epinglage d'asn1-ber sur un commit
       (v1.5.8-0.20250403…) n'avait pas de raison ecrite parce qu'il n'etait
       pas le notre — c'etait l'exigence de go-ldap v3.4.12 (lue dans son
       go.mod). Parti avec elle : le core passe a v1.5.8, comme Nexus.
    -3 YAML v3 PARTOUT (decision du 24/09, sur recommandation) : socle Ducky,
       agent, proxy, Nexus. Nexus : UnmarshalStrict -> decodeur KnownFields,
       et io.EOF d'un fichier vide traite comme avant (les defauts). Socle :
       ecriture en indentation 2 (enYAML), celle de v2 et du core.
    -4 CONTROLE DIFFERENTIEL fait ici : chaque fichier YAML d'exemple du depot
       (proxy, Nexus x2, ducky.example, un client_software.yaml au format du
       core avec « isServeur: yes ») decode en v2 PUIS en v3 dans les memes
       structures : resultats identiques, message de refus d'un champ inconnu
       compris. Seule difference, a l'ECRITURE : v3 indente les listes sous
       leur cle (« - ip » devient « ␣␣- ip ») ; pas d'option pour l'eviter.
    -5 Changement de comportement assume : une cle en DOUBLE dans un fichier
       retouche a la main etait acceptee par v2 (la derniere gagnait), elle
       est refusee par v3, avec sa ligne. Dans A_TESTER.md § 20c.
    -6 Plus aucune divergence declaree : les trois lignes « ! » de
       dependances.roles sont retirees, page regeneree (17 -> 13
       bibliotheques).
    -7 Trouve en chemin et corrige : automatisation/dependances.sh etait en
       mode 644 dans Git, alors que la doc dit de le lancer par
       ./automatisation/dependances.sh — « Permission denied ».

    VERIFICATION :
       - go build, go vet, go test -race : verts sur les neuf modules ;
         GOOS=windows go build et go vet sur l'agent Windows et le socle.
       - tests ajoutes : Nexus refuse un champ inconnu et accepte un fichier
         vide ; le socle relit ce qu'il ecrit, indente de deux ; l'agent lit
         « isServeur: yes » comme vrai.
       - NON eprouve : vlt distant avec le nouveau x/crypto, et le redemarrage
         d'un parc reel sur ses fichiers existants. A_TESTER.md § 20c.

91.[FAIT-IA] [LOGS] Journal commun des cores, en base
       Constat (recette du 24/09) : chaque core journalisait chez lui ; avec
       plusieurs cores, il fallait deviner lequel avait traite la requete, et
       le portail n'affichait que les journaux du core qui le servait.
       Decision du 24/09 : une table dediee en base plutot qu'un service de
       journalisation.

    -0 Table server_logs (core/database/db_journaux/schema.go) : horodatage
       DATETIME(6) en UTC, severite, niveau, code, CORE EMETTEUR, message,
       request_id, user_id. core_name en texte sans cle etrangere : un core
       retire du cluster laisse ses lignes. Quatre index, chacun sur
       (critere, created_at) : on filtre toujours sur une periode.
    -1 Le paquet logs ne peut pas importer la base (la base l'importe) :
       logs.BrancherSortie, une fonction branchee par main — meme motif que
       permission.SetRevokedChecker. Le nom du core est logs.NomDuCore(), le
       MEME os.Hostname() que l'inscription au cluster.
    -2 L'ecriture ne bloque JAMAIS l'appelant : file bornee (10 000), une
       goroutine insere par lots de 200, au plus une seconde de retard. File
       pleine ou base refusee : lignes comptees perdues pour la base (elles
       restent sur stdout), et WARNING VLT-LOG001 au plus une fois par minute
       avec le compte et la cause. Pas de nouvel essai : il accumulerait en
       memoire ce que la file bornee existe pour eviter.
    -3 Ce qui part en base : TOUT SAUF DEBUG (dbjournaux.VaEnBase). Pas de
       seuil plus haut : l'audit des ecritures est en INFO. Le DEBUG est le
       niveau le plus bavard et ne s'allume que pour un diagnostic.
    -4 Retention parametrable dans core/reglages : log_retention_days (1 a 365,
       30 par defaut, comme les archives fichier et les metriques) et
       log_purge_hours (24 par defaut). Nouvelle unite « j » dans reglages.
       Purge au demarrage PUIS par reglages.Boucle ; par lots de 10 000,
       enchaines jusqu'a un lot incomplet (plafond 100 lots par passage) — un
       lot par jour ne rattraperait jamais un emballement. Chaque core purge
       sans coordination : deux DELETE identiques ne font rien de plus.
    -5 Lecture : une action log.list (read:log, PorteeGlobale,
       UnDomaineSuffit) pour les DEUX facades. Filtre par SEUIL de gravite,
       core, code, periode (absolue ou relative : 2h, 30m, 3j), pagination
       sans COUNT(*) (une ligne temoin dit s'il y a une suite). Base
       injoignable : repli sur la memoire du core, ANNONCE comme tel. Un test
       confronte le filtre SQL et le filtre memoire sur les memes cas.
    -6 CLI : nouvelle commande « logs » (command_logs). Option inconnue
       REFUSEE, pas ignoree. La reponse donne la commande exacte de la page
       suivante.
    -7 Portail : /admin/api/logs passe par log.list (il lisait la memoire du
       core qui servait la page). Filtres gravite / core / code / periode,
       pagination, bandeau de repli, Cache-Control: no-store. Les champs
       datetime-local sont envoyes en RFC 3339 : a l'heure du navigateur, le
       core les aurait lus a la sienne (souvent UTC en conteneur).
    -8 Trouve en chemin et corrige : settings.list/set/reset n'etaient dans
       AUCUNE des deux tables de portees_declarees_test.go — le lot des durees
       manquait a enregistrerToutDans. Ajoute, avec log.list. Et le tableau du
       § 22 de MAN.md ignorait gpo_refresh_minutes : ajoute.
    -9 Trouve en chemin, NON traite : un core ne redemarre pas sur une base
       existante quand clientconfpath n'est pas /opt/vaultaire/ -> TO-DO 94.
       Reproduit sur le commit de depart, sans rapport avec ce point.

    NON TRAITE :
       - pas de plafond en NOMBRE de lignes : la table est bornee par le temps.
         Un emballement de plusieurs jours la grossit d'autant jusqu'a la purge
         suivante. La file bornee limite le debit par core, pas le total.
       - les lignes emises avant le branchement (lecture de la configuration,
         ouverture de la base) ne sont que sur stdout.
       - les journaux des agents, du proxy et de Nexus ne sont pas centralises.

    VERIFICATION :
       - go build, go vet, gofmt : propres sur vaultaire_serveur.
       - go test -race ./... : vert sur vaultaire_serveur, y compris les tests
         d'integration de db_journaux contre MariaDB 10.11 en mode SQL strict
         (VAULTAIRE_TEST_DSN) : aller-retour fidele (microsecondes, UTC,
         troncature UTF-8), filtres, pages, purge, ecrivain.
       - e2e fait ici sur un vrai core (sans LDAP, DNS, web ni API) : les lignes
         du demarrage arrivent en base signees du core ; « logs --level
         WARNING », « --code », « --since », « --per-page » et la suite
         proposee repondent juste ; « --since hier » et « --sinec » refuses ;
         une ligne vieille de 40 jours est purgee au redemarrage.
       - portail eprouve dans Chromium (Playwright), navigateur en
         Europe/Paris face a un core en UTC : connexion, page Logs, liste des
         cores, filtre WARNING, periode d'une heure autour de maintenant
         (toutes les lignes rendues : le decalage de fuseau est bien absorbe),
         fin avant debut -> message de l'action affiche ; 130 lignes d'un
         second core injectees en SQL : le menu Core le propose, et les trois
         pages rendent les 130 lignes, sans doublon ni trou.
       - NON eprouve : deux VRAIS cores sur une meme base, coupure de la base.
         Voir A_TESTER.md § 21.

80.[FAIT-IA] [DEP VERSION] Inventaire des dependances, et ce qu'il a revele
       Demande de Lorens (24/09) : « pouvoir gerer facilement la liste des
       dependances du projet et aussi a quoi elles servent ».

    -0 Constat : neuf go.mod, deux images de base, des paquets systeme dans les
       Dockerfiles. Rien qui dise en un seul endroit de quoi le produit depend,
       et surtout rien qui dise POURQUOI. Savoir qu'on tire pkg/sftp n'apprend
       rien ; savoir qu'elle porte « create -c … --join » dit tout de suite si
       on peut s'en passer.
    -1 Partage : ce qui se LIT dans les fichiers est genere (versions, modules,
       images, paquets), ce qui ne s'y lit pas est ecrit a la main
       (docs/Developement/dependances.roles). Une page entierement tenue a la
       main aurait diverge — c'est ce qui avait produit la liste de modules
       fausse de dev.yaml. Une page entierement generee n'aurait eu aucun
       endroit ou expliquer quoi que ce soit.
    -2 Nouveau paquet core/dependances : il SERT DEUX FOIS, pour produire la
       page et pour la verifier. Refaire l'analyse en bash aurait donne deux
       implementations a tenir d'accord, donc tot ou tard deux reponses a la
       meme question. automatisation/dependances.sh ne fait que trois lignes.
    -3 Cinq tests : la page decrit-elle encore le depot (comparaison du bloc
       delimite, avec la premiere ligne qui differe en message d'echec) ; chaque
       dependance a-t-elle une explication ; une explication survit-elle a la
       dependance qu'elle decrivait ; une divergence de version est-elle
       declaree ; une justification survit-elle a la divergence. Plus un garde
       contre l'inventaire vide — un scan casse rendrait « aucune dependance »
       et le test passerait.
    -4 Les divergences ne sont pas interdites, elles doivent etre ECRITES avec
       leur raison. Une divergence qu'on a decide d'accepter et une divergence
       qu'on n'a pas vue se ressemblent beaucoup, et c'est la seule chose qui
       les separe.
    -5 Le scan des Dockerfiles est HONNETE SUR SES LIMITES, et la page le dit :
       un go.mod est lu par l'outil qui construit, une ligne « dnf install » est
       du shell. Defaut trouve et corrige en chemin : un COMMENTAIRE de
       deployments/dev/dockerfile (« NON de `dnf install golang` ») etait releve
       comme un paquet installe. Un inventaire qui invente une dependance est
       pire qu'un inventaire qui en oublie une.
    -6 Format du fichier des roles volontairement pauvre — « chemin = role »,
       « !chemin = raison de la divergence », « # » en commentaire. Ni YAML ni
       JSON : il se relit dans un diff, s'edite sans echapper quoi que ce soit,
       et n'ajoute aucune dependance a l'outil qui inventorie les dependances.
    -7 TROIS DEFAUTS REVELES, consignes en TO-DO 93 : golang.org/x/crypto en
       trois versions (v0.47.0 / v0.52.0 / v0.54.0) alors que c'est la
       bibliotheque de crypto ; github.com/go-ldap/ldap/v3 declaree directe mais
       importee NULLE PART, qui tire trois dependances pour rien ; et deux
       versions de YAML dans le meme produit (v3 au core, v2 partout ailleurs).
    -8 e2e fait ici : le garde-fou mord (version modifiee a la main dans la page
       -> echec avec la ligne fautive et la commande a lancer), et la
       regeneration remet en etat.

92.[FAIT-IA] [SESSIONS] Table dediee aux sessions utilisateur
       Recette du 24/09 : « une ligne dans status -c = une session ducky, or
       quand une session user est detectee cela cree une entree aussi dans
       status -c ». Regression de MA premiere version du point 68.

    -0 Constat : Command_STATUS_GetClientsConnected lit did_login. Le point 68 y
       ecrivait les sessions PAM faute de table ou les mettre : une machine avec
       quelqu'un connecte apparaissait DEUX fois, pour un seul tunnel.
    -1 Nouvelle table user_sessions (decision du 24/09). did_login redevient
       strictement « une ligne = une session Ducky ». Les deux objets n'ont ni le
       meme cycle de vie, ni la meme cle, ni le meme lecteur.
    -2 Pourquoi PAS une colonne d'origine dans did_login : elle aurait suffi a
       les distinguer, et aurait laisse deux natures dans la meme table plus un
       filtre a ne pas oublier. La lecture suivante serait retombee dedans.
    -3 L'unicite (compte, machine) est portee par la BASE cette fois, pas par le
       code comme dans did_login. INSERT ... ON DUPLICATE KEY : deux connexions
       du meme compte sur la meme machine (console + SSH) peuvent arriver
       ensemble.
    -4 Rebranche : 03_01 ouvre, 03_11 ferme, le battement 02_12 prolonge
       user_sessions SEULEMENT (l'etendre a did_login maintiendrait en vie les
       lignes fantomes du 68), la purge enchaine sur les deux tables, et les
       trois lectures de status -u passent par une requete COMMUNE — trois
       requetes ecrites a la main finissaient par diverger sur une jointure.
    -5 AUCUNE MIGRATION. Les lignes ecrites par le 68 vivent dans did_login avec
       dix minutes de validite et plus rien ne les prolonge : la purge les
       emporte d'elle-meme. Les distinguer d'une session 02_03 legitime aurait
       ete plus risque que de les laisser s'eteindre.
    -6 Deux ajouts de coherence : la revocation retire les sessions utilisateur
       (un tableau de bord qui montre un compte coupe comme connecte ment, et
       c'est au moment d'un kill switch qu'on le regarde), et « connecte » dans
       get_user_info couvre les DEUX tables — repondre « non » a propos de
       quelqu'un assis devant sa machine est la reponse la plus fausse possible.
    -7 Trois lectures did_login mortes supprimees (status_get_connected_user*,
       status_get_users_by_group), ainsi que ProlongerSessionsDeLaMachine.
    -8 Tests-sentinelles : ils analysent la SOURCE et n'inspectent que les
       litteraux chaine — les deux fichiers se citent en commentaire, et c'est
       voulu. status -c ne doit jamais nommer user_sessions, sessions_utilisateur
       ne doit jamais nommer did_login, la purge doit couvrir les deux, et
       l'echeance doit venir de la constante partagee.

83.[FAIT-IA] [WINDOWS] Le refus de mot de passe dit enfin qui refuse
       Recette du 24/09 : « lors de la creation du compte NetUserAdd:code 2245
       parametre 4294967295 ».

    -0 Diagnostic : 2245 = NERR_PasswordTooShort, dont le NOM MENT — Windows le
       rend pour tout refus de la politique de mot de passe : longueur,
       complexite, ou mot de passe encore dans l'historique. Le parametre
       0xFFFFFFFF est PARM_ERROR_UNKNOWN, « je ne sais pas quel champ ». C'est
       la politique DU POSTE qui refuse le mot de passe du domaine. D'ou la
       dissymetrie observee : -check ne cree aucun compte, donc ne la rencontre
       jamais.
    -1 L'agent ne peut PAS choisir ce mot de passe : c'est celui que la personne
       tape a l'ecran de connexion, et Windows le verifie contre le compte
       local. En poser un autre rendrait la connexion impossible. Le correctif
       ne pouvait donc pas etre technique, seulement explicatif + outillage.
    -2 Nouveau compte/erreurs.go, SANS build tag windows : il ne contient que des
       nombres et des phrases, donc il s'eprouve sans machine Windows — et c'est
       justement la partie qu'on veut eprouver, un message ne se verifiant qu'en
       le lisant. NetUserAdd et NetUserSetInfo y passent tous les deux : un
       changement de mot de passe cote domaine rencontre la meme politique.
    -3 Le message nomme QUI refuse (le poste, pas Vaultaire), les TROIS causes
       possibles, et quoi faire. Le parametre fautif n'est affiche que s'il veut
       dire quelque chose.
    -4 install.ps1 affiche la politique locale et PROPOSE de l'aligner :
       net accounts pour longueur et historique, secedit pour la complexite qui
       n'est pas exposee autrement. Rien n'est assoupli sans accord — c'est une
       politique de securite du poste. Avertissement affiche, et note sur les
       machines jointes a un AD, ou la strategie de domaine ecrase la locale.
    -5 Le fichier secedit est reecrit en UTF-16 : c'est LE cas ou c'est correct,
       secedit produit et attend de l'Unicode. La regle du point 81 n'est pas
       « toujours de l'UTF-8 », c'est « l'encodage que le lecteur attend ».
    -6 Tests : le message designe la politique du poste, cite les trois causes,
       dit quoi faire ; le parametre inconnu n'est pas affiche mais un vrai
       indice l'est ; un acces refuse oriente vers le compte du service et non
       vers le mot de passe ; un code inconnu reste une erreur ; zero n'en est
       pas une.

88.[FAIT-IA] [GPO] Scope user : proprietaire et groupe retires
       Recette du 24/09 : « creation d'un dossier, parametre a remplir proprio et
       groupe, aucun sens, cela doit etre le user lui-meme ».

    -0 Constat : file_deploy, directory_manage et templated_file_deploy sont des
       modules ScopeBoth qui demandaient owner/group dans les DEUX scopes. En
       scope user l'agent pose lui-meme le proprietaire (writeUserFile) : root
       avait ete saisi, le dossier a ete cree au nom de l'utilisateur — le bon
       comportement, avec un champ qui affirmait le contraire.
    -1 Le catalogue ne savait restreindre qu'un MODULE entier. Nouveau champ
       FieldSchema.Scope (vide = partout), + FieldsForScope et AllowedInScope.
       Un champ qui ne change rien est pire qu'un champ absent : il se remplit,
       il se relit, et on lui prete un effet qu'il n'a pas.
    -2 Le filtre s'applique en QUATRE endroits, et les quatre comptent :
       buildFieldViews (le champ n'est plus propose), ParametresDeModule (sinon
       il serait reecrit vide en base a chaque modification et survivrait a son
       propre retrait), ValidateModule (plus recopie dans les params retenus),
       moduleSummary (une GPO ancienne n'affiche plus « Proprietaire = root »).
    -3 MIGRATION : un parametre hors scope trouve en base est IGNORE, jamais
       refuse. ValidateModule est rappelee sur les modules VOISINS a chaque
       modification d'une GPO : le refuser rendait ces politiques immodifiables
       tant que personne n'avait nettoye la base a la main. Un parametre qui
       n'existe nulle part au schema reste refuse.
    -4 L'empreinte des GPO user concernees change une fois (les cles quittent
       les params retenus) : le parc user reapplique une fois, puis se tait.
    -5 Cote agent : RIEN a changer, les deux applicateurs gardaient deja
       ScopeUser. C'etait bien un defaut d'interface, pas de comportement.
    -6 Tests : les champs partent en user et restent en machine, le reste du
       module ne part pas avec eux, une GPO user ancienne reste modifiable sans
       recopier les cles, un parametre vraiment inconnu reste refuse.

87.[FAIT-IA] [GPO] trusted_ca : la famille se reconnait a sa COMMANDE
       Recette du 24/09 : « trusted_ca, update-ca-certificate existe pas dans le
       path (machine de test Rocky 9) ».

    -0 Constat : detectCAStore retenait le premier REPERTOIRE existant, Debian en
       tete de liste. Or /usr/local/share/ca-certificates est un repertoire
       ordinaire sous /usr/local, que n'importe quel paquet peut creer : une
       Rocky 9 etait prise pour une Debian et l'agent lancait
       update-ca-certificates, absent de cette distribution.
    -1 La detection porte desormais sur la COMMANDE (commandExists, deja
       injectable). Un magasin n'est utilisable que si le programme qui le
       COMPILE est la : deposer le fichier ne suffit pas a rendre la CA
       effective. Un repertoire, lui, ne prouve rien.
    -2 Le repertoire garde un role : departager deux familles dont les deux
       commandes sont installees. Et s'il manque alors que la commande est la,
       il est cree — c'est un emplacement documente de la distribution, pas un
       chemin invente.
    -3 L'echec NOMME ce qui a ete cherche (familles + commandes). Le message
       d'origine ne le disait pas : c'est pourquoi ce defaut a coute une session
       de recette au lieu d'une lecture de journal.
    -4 Type magasinCA nomme, au lieu du struct anonyme repete trois fois.
    -5 Tests : une Rocky avec le repertoire Debian reste une Rocky (LE cas de la
       recette), le repertoire departage quand les deux commandes existent, le
       repertoire d'ancrage manquant est cree, aucune commande = refus, et le
       message nomme les familles.

81.[FAIT-IA] [WINDOWS] Encodage : deux regles opposees, et les deux sont justes
       Recette du 24/09 : « pb d'encodage sur client_conf.json et sur
       install.ps1 ».

    -0 Deux defauts INVERSES, ce qui explique qu'ils soient apparus ensemble.
       Windows PowerShell 5.1 — celui livre avec Windows — lit un .ps1 SANS BOM
       comme de l'ANSI (Windows-1252) : tous les accents du script devenaient
       des « Ã© ». Et « Set-Content -Encoding UTF8 » ECRIT un BOM, que la norme
       JSON interdit : le decodeur Go refusait client_conf.json en se plaignant
       d'un « caractere invalide 'i' », ce qui n'evoque rien pour personne.
    -1 build.sh ajoute le BOM aux deux .ps1 en les copiant dans l'archive. Les
       sources du depot restent sans BOM, pour que git diff et grep restent
       lisibles.
    -2 install.ps1 ecrit par EcrireTexte ([System.IO.File]::WriteAllText avec un
       UTF8Encoding($false)) : ni Set-Content -Encoding UTF8 (BOM en 5.1), ni
       « > » ou Out-File sans option, qui ecrivent en UTF-16LE. Les deux
       ecritures du script y passent : la configuration et l'empreinte du core.
    -3 L'agent TOLERE un BOM en tete de client_conf.json (sansBOM dans
       config/loadConfig.go). Le fichier sera rouvert dans le Bloc-notes pour
       ajouter un core, et refuser une configuration valide pour trois octets
       invisibles serait une mauvaise maniere de faire respecter la norme. Le
       client Windows passe par le meme LoadConfig : un seul correctif.
    -4 Un JSON reellement malforme reste refuse — on n'a pas remplace un message
       obscur par un silence.
    -5 Tests : configuration avec BOM lue, sans BOM inchangee, JSON malforme
       toujours refuse (dont un double BOM : une seule marque est retiree).
       Verifie aussi que l'archive produite porte bien le BOM et que les sources
       du depot ne l'ont pas.

75.[FAIT-IA] [CI] Les tests tournent a chaque merge dans dev
       Demande de Lorens (23/09) : « traite le point 75 qui doit se lancer a
       chaque merge d'une branche dans dev seulement ».

    -0 Constat : dev.yaml lancait gofmt, go vet, golangci-lint, gosec, semgrep,
       govulncheck et Trivy — jamais go test. Rien n'empechait un test rouge
       d'etre pousse, et c'est ainsi que des tests perimes ont vecu des mois.
    -1 Nouveau .github/workflows/tests.yaml. Declenchement : push sur dev
       UNIQUEMENT (un merge dans dev se voit comme un push), plus
       workflow_dispatch pour rejouer a la main. Ni pull_request ni cron : une
       branche de travail a le droit d'etre rouge, et un test ne change pas
       d'issue parce qu'il est lundi.
    -2 Par module : go build, go vet, go test -race. Matrice fail-fast: false —
       un module rouge ne doit pas masquer l'etat des autres.
       cancel-in-progress: false, contrairement a l'audit : deux merges
       rapproches donneraient sinon un seul verdict, et le premier resterait
       sans reponse.
    -3 -race PARTOUT, et non « au moins le SDK et le proxy » comme le disait le
       TO-DO : une course est le defaut qu'un test ne trouve QUE si on le lui
       demande. Le prix est quelques minutes, sur un declenchement rare.
    -4 Job de compilation croisee GOOS=windows (agent Windows + face Windows du
       socle) : Linux ne compile meme pas ces fichiers, on pouvait donc les
       casser sans qu'aucune execution ne rougisse.
    -5 La liste des modules n'est plus ecrite a la main, ni ici ni dans
       dev.yaml : elle vient de « find src -maxdepth 2 -name go.mod ». L'ancienne
       citait vaultaire_api (inexistant — le pas etait SAUTE) et en oubliait
       quatre. Une decouverte qui ne trouve rien ECHOUE, au lieu de rendre une
       matrice vide, c'est-a-dire un vert qui n'a rien teste.
    -6 Troisieme defaut, trouve en chemin : le job lint de dev.yaml etait
       TOUJOURS vert. Ses trois pas portent continue-on-error (voulu, pour que
       les rapports sortent) mais personne ne relisait leur issue. Ajout d'un
       pas « Verdict du lint » : gofmt et go vet bloquent, golangci-lint non
       (version: latest, son verdict peut changer sans que le depot bouge).
    -7 Sort de api_client_package tranche : il reste, et il est desormais
       compile et analyse. Un exemple publie qui ne compile plus est un exemple
       qui ment. Il n'a pas de tests, go test ./... se contente donc de le
       construire.
    -8 ATTENTION : le depot protege .github/. Le fichier neuf et les deux
       corrections de dev.yaml sont appliques dans la copie de travail, mais la
       mise en place se fait a la main. Les huit modules passent
       go test -race ./... ici : la premiere execution devrait etre verte.

68.[FAIT-IA] [SESSIONS] status -u voit enfin les sessions PAM
       Demande de Lorens (23/09) : « et aussi traite le point 68 ».

    -0 Constat : depuis la suppression du defi, l'auth PAM passe dans le tunnel
       MACHINE (03_01) et aucune ligne did_login n'etait ecrite. status -u ne
       listait que des lignes « vaultaire » — une par machine — et jamais Alice
       sur web01. Le core savait qui il venait de laisser entrer et l'oubliait
       dans la seconde.
    -1 Ouverture : une 03_01 qui franchit TOUS les controles ecrit la ligne
       did_login (AddLoginEntry, apres les controles — une ligne ecrite plus tot
       montrerait dans status -u des gens a qui l'acces a ete refuse). La cle
       enregistree est celle du tunnel machine : une session PAM n'en a pas a
       elle, et c'est bien sous celle-la que l'authentification a voyage.
    -2 Fermeture : nouvelle trame 03_11 (agent -> core), emise par
       handleCloseRequest. PAS de 02_05, qui signifie « ferme cette connexion »
       et avait coupe le tunnel machine a chaque deconnexion (point 64). Aucune
       reponse : l'agent n'a aucune decision a prendre selon l'issue.
    -3 La MACHINE vient de l'en-tete authentifie, jamais du contenu : un agent
       ne peut fermer que des sessions ouvertes chez lui. Sinon n'importe quel
       poste effacait les sessions du parc. Le compte « vaultaire » est refuse
       explicitement — l'effacer ferait disparaitre la machine de status -c.
    -4 Expiration : le battement de la machine (02_12) prolonge TOUTES les
       lignes du poste, la sienne comme celles de ses utilisateurs
       (ProlongerSessionsDeLaMachine). Une session utilisateur n'a pas de
       battement a elle ; sans cela elle disparaissait en dix minutes alors que
       la personne est devant son ecran. Contrepartie assumee : une 03_11 perdue
       laisse la ligne tant que la machine tourne.
    -5 03_11 ajoutee au catalogue clienttype pour vaultaire_client seul. Sans
       cette entree, le Spliter FERME la connexion de l'agent : le symptome
       aurait ete une machine qui perd son tunnel chaque fois que quelqu'un se
       deconnecte.
    -6 status -u gagne une colonne MACHINE. Sans elle, une personne connectee
       sur trois postes donnait trois lignes identiques, et « vaultaire » une
       par machine, toutes indiscernables. Une session, c'est qui ET ou.
    -7 Deux defauts trouves en chemin et corriges : la validite de 10 minutes
       etait ecrite en dur dans TROIS fichiers (une constante ValiditeSession et
       EcheanceSession les remplacent) ; et status -u -g scannait des colonnes
       NULLABLES (LEFT JOIN did_login) dans des chaines Go — un seul membre du
       groupe deconnecte faisait echouer toute la commande.
    -8 Non traite, dit dans la doc : un poste WINDOWS n'emet pas 03_11 (pas de
       PAM), ses sessions restent affichees jusqu'a l'extinction. Releve dans
       Installation/Client_Windows.md et rattache au TO-DO 79.
    -9 Tests : refus du tunnel machine, contenu vide ou tronque, lecture de la
       premiere ligne seule, et presence de 03_11 au catalogue pour l'agent et
       pour lui seul. e2e a faire (A_TESTER § 16).

51.[FAIT-IA] [GPO] [REGLAGES] Cadence de rafraichissement decidee par le core
       Demande de Lorens (23/09) : « point 50 51 a faire ».

    -0 Constat : MachineRefreshInterval etait une constante (1h) dans l'agent.
       Le point 13 avait sorti les autres durees du code ; celle-ci n'avait pas
       suivi, et db_gpo en portait une SECONDE copie pour juger une machine
       « en retard » — deux valeurs a tenir d'accord, dont aucune reglable.
    -1 Nouveau reglage gpo_refresh_minutes (core/reglages), 60 par defaut,
       borne 5..1440, avec sa Consequence.
    -2 La valeur part vers les agents en QUEUE des reponses machine 05_02 et
       05_03, sur une ligne prefixee « refresh: ». Meme recette que sync: en
       03_09 : ajoutee en queue, reconnue au PREFIXE et jamais au rang, donc un
       agent ancien l'ignore et un core ancien ne l'envoie pas. Elle part aussi
       dans 05_03 — « rien a faire » est le cas frequent, donc le seul chemin
       vers un parc dont la politique ne bouge pas.
    -3 Aucun scope user : un cycle utilisateur est declenche par une ouverture
       de session, pas par une boucle. Un reglage qui ne pilote rien serait pire
       que pas de reglage.
    -4 Cote agent : cadence.go, valeur BORNEE a la reception (5min..24h) — elle
       vient du reseau et pilote une boucle infinie ; zero aurait donne une
       attente active, un mois aurait fait disparaitre la machine du parc. Non
       persistee : le premier cycle est immediat, la fenetre dure un aller-retour.
    -5 La duplication est supprimee : db_gpo.CadenceAgent est une fonction posee
       au demarrage du core depuis le reglage, et ToleranceRapport() vaut trois
       cycles. La colonne SUIVI suit donc la cadence reelle au lieu de mentir
       des le premier changement.
    -6 Tests : cadence lue a n'importe quel rang, bornes, absence de ligne
       (defaut conserve), reveil de la boucle sur changement seulement ; cote
       core, rang des six champs du manifeste preserve et absence en scope user.
    -7 Docs : 05-gpo/01 et 02 (plage 05_01..05_18, ligne refresh:),
       Reglages_de_duree.md § 6 bis — le paragraphe qui disait « les durees de
       l'agent ne sont pas reglables » etait devenu faux.

50.[FAIT-IA] [GPO] Declencher un cycle hors du tour periodique
       Demande de Lorens (23/09) : « point 50 51 a faire ».

    -0 Constat : un cycle ne partait qu'au demarrage puis a chaque tour. Pas
       d'equivalent de gpupdate /force ; une reconnexion apres deux heures de
       coupure attendait le tour suivant ; un cycle en echec aussi.
    -1 Trame 05_18 (core -> agent), la SEULE de la categorie que le serveur
       emette de lui-meme. Elle ne transporte AUCUNE politique : l'agent repart
       sur une 05_01 ordinaire. Une trame de reveil ne pouvait pas devenir un
       second chemin d'application a tenir d'accord avec le premier.
    -2 Rien n'est mis en file pour une machine hors ligne, contrairement a un
       ordre de revocation : elle fait de toute facon un cycle en revenant.
    -3 CLI : vlt gpo refresh <computeur_id> | --all. Le droit est
       write:update:client sur les domaines de la MACHINE, et non
       write:update:gpo : ce qu'on engage est le poste et ses services, pas la
       politique — sinon quiconque edite une GPO lancerait un cycle sur tout le
       parc, hors de sa delegation. --all boucle sur les machines connectees et
       annonce ce qui est hors perimetre.
    -4 Cycle a la RECONNEXION : une goroutine compare la cle de session mere a
       celle du dernier cycle. Scrutation et non signal, comme waitForSessionKey
       — cabler une notification dans la couche transport pour un seul
       consommateur l'aurait fait dependre du paquet gpo.
    -5 Nouvel essai rapproche apres un echec : la boucle passe d'un ticker a un
       minuteur reame, avec le paquet backoff du socle. Le plafond du backoff
       (5 min) vaut la cadence minimale, donc un reessai n'est jamais plus
       espace qu'un tour normal.
    -6 Le ticker ne pouvait porter aucune des trois : cadence changeante, reveil
       hors tour, reessai rapproche. Le minuteur rend les trois possibles.
    -7 e2e a faire (A_TESTER) : refresh sur une machine, sur --all, refus pour
       un delegue, et coupure de tunnel.

78.[FAIT-IA] [WINDOWS] [INVENTAIRE] L'agent Windows remonte son inventaire systeme
       Demande de Lorens (23/09) : « traite le point 78, il doit remonter des infos
       systeme lui aussi ».

    -0 Constat : les colonnes SYSTEME, RAM et CPU restaient vides pour un poste
       Windows. Les collecteurs du socle lancent « cat /etc/os-release »,
       « free -h », « nproc » et « who » : aucun de ces programmes n'existe la.
    -1 Nouveau fichier inventaire_windows.go dans le socle (getlocalinformation),
       les collecteurs UNIX passant sous « //go:build !windows ». API et NON
       commandes : wmic est retire des versions recentes, la sortie de
       systeminfo/query user est TRADUITE (analyser « Memoire physique totale »
       marche en francais et nulle part ailleurs), et un service SYSTEM n'a pas
       a lancer un processus par inventaire.
    -2 Systeme : RtlGetVersion (et non GetVersionEx, qui MENT et rend 6.2 sans
       manifeste) pour le build, + registre pour l'edition et la version
       commerciale. Le registre annonce « Windows 10 Pro » sur un Windows 11 :
       corrige par le build (>= 22000). L'EditionID n'est PLUS colle au
       ProductName, qui la porte deja sous un autre mot — cela donnait
       « Windows 10 Pro Professional ».
    -3 Memoire : GlobalMemoryStatusEx, rendue au format « total/utilisee » de
       free -h (FormatOctets, teste). Separateur DECIMAL : le point, comme un
       service systemd en locale C — une virgule aurait melange deux ecritures
       dans la meme colonne du core.
    -4 Processeurs : GetActiveProcessorCount(ALL_PROCESSOR_GROUPS) et non
       runtime.NumCPU : au-dela de 64 processeurs logiques, Windows les range en
       groupes et un processus ne voit que le sien — un bi-socket serait annonce
       avec la moitie de ses coeurs.
    -5 Sessions ouvertes : WTSEnumerateSessions + WTSQuerySessionInformation,
       sessions actives ET deconnectees (une session verrouillee existe bel et
       bien). Pointeurs types et unsafe.Slice : « go vet » signalait a raison
       l'arithmetique sur uintptr.
    -6 Le socle depend desormais de golang.org/x/sys (deja utilise par l'agent
       Windows). Les quatre modules qui l'importent compilent.
    -7 e2e : agent Windows sous wine contre un core reel — « vlt get -c » affiche
       « Windows 10 Pro (build 19043) », « 7.8Gi/976Mi », « 2 », a cote de la
       ligne Ubuntu et dans le meme format. Non verifie : un vrai Windows 11
       (nom, version commerciale), et l'enumeration des sessions, que wine
       n'implemente pas.

77.[FAIT-IA] [WINDOWS] Client Windows V1 — authentification par mot de passe
       Demande de Lorens (23/09) : « dev une V1 du client windows, dossier avec
       script bash qui compile une archive .tar, installation manuelle, valeurs de
       conf demandees par l'installeur ; comportement d'un client classique en auth
       par mot de passe ; trames GPO recues mais pas traitees ; le reste OUI, dont
       la gestion de la liste des core/proxy avec les priorites ; credential
       provider comme JumpCloud, agent Go + agent C, GRPC ? ».

    -0 Architecture retenue, et l'arbitrage sur l'IPC : DLL COM (Credential
       Provider) chargee dans LogonUI.exe + service Go, reliés par un TUBE NOMME
       et non gRPC. Ce qui plante dans LogonUI fait disparaitre l'ecran de
       connexion : la DLL doit rester minuscule (aucune pile protobuf/TLS), et un
       tube n'ouvre AUCUN port — un socket loopback est joignable par tout
       utilisateur du poste, or le mot de passe en clair y passe. DACL
       D:P(A;;GA;;;SY)(A;;GA;;;BA) : meme decision que le socket PAM Linux.
       Format des messages : celui du canal PAM (JSON plat), pas un second.
    -1 src/vaultaire_client_windows/ (module Go) : ipc (protocole + serveur de
       tube + client), auth (03_01, attente du verdict, service de traitement),
       compte (nommage deterministe + NetUserAdd/NetUserSetInfo/
       NetLocalGroupAddMembers), gpo (trames 05 et 06 recues et IGNOREES, mais
       journalisees), version, main (service Windows ou console).
    -2 Reemploi plutot que copie : config, serveur_communication (tunnel,
       supervision, backoff), pamstate et sshreq viennent du module de l'agent
       Linux par un replace. Meme fichier client_conf.json, meme section
       « learned » (TO-DO 61), donc meme comportement de cluster : ordre servi,
       affinite, priorite, repli sur « servers ».
    -3 Compte local : nom deterministe « alice-1f4b2c » (partie locale + 6
       caracteres d'empreinte du nom complet, minuscules, 20 caracteres max).
       Deux domaines homonymes -> deux comptes ; un compte local preexistant
       n'est JAMAIS repris ; mot de passe realigne a chaque connexion reussie.
       Groupes resolus par SID (S-1-5-32-545/544) et non par nom, qui est traduit.
    -4 credential_provider/ : DLL COM complete (fabrique, ICredentialProvider,
       ICredentialProviderCredential2, serialisation KERB_INTERACTIVE_UNLOCK_LOGON
       avec pointeurs relatifs, inscription HKLM). Compilee ICI avec MinGW —
       4 exports, aucune dependance hors DLL systeme. Recette MSVC documentee.
    -5 build.sh -> dist/vaultaire-client-windows-<version>.tar (agent,
       vaultaire_login, DLL, install.ps1, uninstall.ps1, LISEZMOI, exemple de
       conf). install.ps1 demande identite, cores, empreinte du core, service,
       tuile ; pose les ACL sur C:\ProgramData\Vaultaire.
    -6 e2e REEL (core + MariaDB + agent Windows sous wine) : tube et protocole
       (etat, indisponible, sans domaine, refus), tunnel etabli, machine visible
       dans « vlt status -c » avec sa version, mot de passe JUSTE accepte par le
       core (03_02, admin false, 1 groupe), mot de passe FAUX refuse, nom de
       compte local rendu. Le provisionnement s'arrete sur la traduction de SID,
       que wine n'implemente pas — a verifier sur une vraie machine (A_TESTER
       § 14).
    -7 DEUX DEFAUTS REELS trouves par le portage, corriges :
       a) yaml.ReadYAMLFile deref un pointeur NUL quand client_software.yaml
          manque : l'agent LINUX paniquait aussi dans ce cas. Rend desormais un
          booleen ; l'agent Linux le journalise, l'agent Windows refuse de
          demarrer.
       b) Get_PublicKeys_ByUserID traitait « aucune cle » comme une ERREUR, et
          le gestionnaire 03_01 en faisait un REFUS : un compte sans cle SSH ne
          pouvait ouvrir AUCUNE session, pas meme par mot de passe. Absence et
          echec ne se confondent plus.
    -8 Non traite : inventaire systeme Windows (TO-DO 78), GPO et revocations
       (TO-DO 79). Non verifie ici : la tuile dans un vrai LogonUI, le service
       Windows, le provisionnement complet.

76.[FAIT-IA] [TESTS] Remise au propre des tests, et documentation
       Demande de Lorens (23/09) : « mets de l'ordre dans l'ensemble des tests, c'est
       100% ton travail et je ne sais pas ce qui a ete fait ni comment cela marche ».

    -0 Etat trouve : `go test ./...` inutilisable. Un paquet ne compilait pas
       (db_gpo : deux aides de test nommees `ligne`), un autre paniquait au
       CHARGEMENT (core/web_serveur : template.Must en variable de paquet, chemin
       relatif au repertoire du paquet), cinq tests paniquaient faute de base, un
       test paniquait en fabriquant lui-meme une empreinte argon2 a parametres
       nuls, et trois decrivaient un contrat perime.
    -1 Repare : db_gpo renomme ; hachage — deux tests SHA-256 reecrits sur
       argon2id et sur security.Verifier ; catalogue d'actions 80 -> 89 avec le
       message qui dit quoi faire ; exigeBase(t) dans core/action (les tests qui
       descendent au SQL se SAUTENT au lieu de paniquer) ; droits du journal SDK
       (le test lisait le mode de t.TempDir(), il eprouve maintenant le
       repertoire CREE par le paquet) ; quota host_handler (fenetre de 400
       caracteres -> bloc du `if`, delimite par ses accolades) ; parametres nuls
       argon2 (empreinte composee sans appeler argon2).
    -2 core/web_serveur : le gabarit de connexion et le chemin MFA etaient figes
       en variables de paquet, donc resolus a l'import — les tests du portail
       etaient tous perdus avant le premier. Resolution a l'usage (sync.Once),
       StartWebServer controle toujours et s'arrete si le gabarit manque ;
       TestMain pose VAULTAIRE_WEB_PACKET en absolu, sans changer le repertoire
       courant (d'autres tests du paquet lisent des sources a cote d'eux).
    -3 Une COURSE reelle trouvee au passage (-race) : le test du superviseur de
       l'agent ecrivait storage.Persistent depuis sa goroutine pendant que la
       boucle supervisee le lisait. La persistance se lit desormais par une
       indirection (estPersistant), le test la pilote par un drapeau atomique et
       attend la fin de la goroutine avant de restaurer les variables.
    -4 gofmt -w sur 15 fichiers non formates (anterieurs).
    -5 Resultat : les 8 modules passent avec -race, sans filtre ni variable.
       466 tests serveur, 169 agent, 72 SDK, 23 Nexus, 10 proxy.
    -6 Documentation : docs/Developement/how it work/Tests.md (commande, les deux
       familles, tests lisant des fichiers, tests-sentinelles, suite --test,
       comment en ecrire un) et Pense-bete_developpement.md (les etapes d'une
       tache, de l'entree TO-DO au commit). Liees depuis les deux index.
    -7 Releve, non traite : TO-DO 75 — l'integration continue ne lance AUCUN test
       (et sa liste de modules pour go vet est fausse). C'est ce qui a laisse ces
       tests rouges vivre des mois. .github/ etant protege, le passage est manuel.

73.[FAIT-IA] [SDK] [CORE] Un enregistrement de noeud refuse ne se voit plus « enregistre »
       Releve lors de l'e2e du relais (22/09), demande de traitement le 23/09.

    -0 Le noeud journalisait « enregistre » des la trame 04_01 EMISE, et le core
       refusant un enregistrement ne repondait RIEN (handler en erreur ->
       message vide). Un proxy pouvait donc paraitre sain sans figurer dans
       cluster_nodes : ni distribue aux agents, ni exempte du plafond par
       adresse. Cas rencontre : hostname deja pris par un autre client.
    -1 Core : RefusEnregistrement compose un 04_02 « refus » + motif d'une ligne
       (propriétaire, type sans role, contenu invalide, port ou empreinte mal
       formes, nom deja pris). Spliter envoie desormais le message MEME en
       erreur quand le gestionnaire en a compose un. Le motif ne dit jamais rien
       d'un autre noeud (« nom deja pris », pas par qui).
    -2 SDK : la 04_01 est suivie d'une ATTENTE de l'accuse (10 s). « Enregistre »
       n'est journalise que sur accuse positif ; un refus part en ERROR avec son
       motif ; le silence (core anterieur) expire en WARNING. Dans les deux cas,
       nouvelle tentative au battement suivant au lieu de battre dans le vide.
    -3 Compatibilite : un accusé reduit a « ok », ou vide, vaut acceptation.
    -4 Tests : accuse ok, refus avec motif, accuse vide, expiration du silence,
       accuse recu alors que personne n'attend (la goroutine de lecture ne doit
       pas bloquer). Sous -race.

74.[FAIT-IA] [DEPLOIEMENT] dev-comp : tester la version en cours sans release
       Demande de Lorens (22/09) : « dans deployments une nouvelle case dev-comp qui
       recompile en local le client, le serveur, le ctl, comme l'ancienne preprod,
       pour tester les versions en cours de dev sans faire de release ».

    -0 deployments/dev-comp/ : dev-comp.sh, docker-compose.yml (projet dev-comp),
       Dockerfile.build (Rocky 9, dependances et Go de la CI de release),
       .env.example, README. Compile TOUT le depot (auto-compil.sh) dans build/,
       ignore par git, distinct de cmd/ : la release installee par la preprod
       n'est pas touchee.
    -1 « Monter une version » : sans --version, la prochaine release de la serie
       (VERSION + plus grand tag local v2.2.N + 1), suivie du commit et de -dirty.
    -2 Pile : base (3307), core avec la MEME image que la preprod et les binaires
       montes, templates du depot montes ; proxy en option (--proxy). Options
       --local, --no-build, --down, --reset, --logs, --status.
    -3 Au passage : l'entrypoint de vlt-proxy ne passait pas -listen-port, le
       proxy de preprod mourait au demarrage. VAULTAIRE_LISTEN_PORT (6666),
       publie sur 6667.
    -4 Non verifie : execution de la pile (registre Docker inaccessible ici).
       Valide : docker compose config, calcul de version (2.2.0 sans tag,
       2.2.4 avec v2.2.3).
    -5 Suite au retour de Lorens (23/09, la compilation et le core fonctionnent) :
       les images du PROXY et du NEXUS sont construites a chaque lancement, meme
       sans --proxy / --nexus — une image qu'on ne construit qu'au moment d'en
       avoir besoin casse au pire moment. Nouveau Dockerfile.nexus propre a
       dev-comp (binaire MONTE, contrairement a src/vaultaire_nexus/deploy/
       qui recompile depuis Docker Hub), service vaultaire-nexus (profil nexus,
       8843, volume devcomp_nexus), option --nexus, nexus/config.yaml minimal
       valide par « vaultaire_nexus -check ». --down, --reset et --status voient
       maintenant TOUS les profils.

38.[FAIT-IA] [DUCKY] [PROXY] Le relais Ducky (lot 4) — partiel, reste au 72
       Demande de Lorens (22/09) : « traite le point 38 partiellement, il ne doit
       faire que relais Ducky pour le moment ; prevois dans la creation relais
       aussi HTTPS et LDAP ; le proxy doit pouvoir faire des relais pas que vers
       du core, par exemple HTTPS vers les Nexus ».

    -0 src/vaultaire_proxy/relais/ : transport TCP sans lecture (arbitrage 2),
       demi-fermeture, inactivite (15 min), plafonds (4000, 20 par source),
       cibles essayees dans l'ordre sans rotation (3 s chacune), REFUS FRANC si
       aucune ne repond, bilan dans le journal toutes les 5 min.
    -1 Configuration : section « relais: » de config.yaml, plusieurs relais.
       Types ducky (actif), https, ldap, ldaps (reconnus, refuses : ErrTypePrevu).
       Sources cores (04_04 puis servers), liste, service:<type> (prevue). Sans
       section : un relais Ducky sur -listen-port. Le relais Ducky doit ecouter
       sur le port annonce.
    -2 SDK : l'empreinte d'un noeud de role proxy n'est JAMAIS apprise (c'est la
       cle de son identite de client ; l'apprendre laisserait un proxy se faire
       passer pour un core). La liste persistee ne garde un proxy que si un core
       de confiance y figure.
    -3 Core : netguard accepte un plafond par source ; 1000 connexions pour les
       adresses des proxies enregistres et en ligne (cluster_nodes, relu toutes
       les 30 s), 20 pour les autres.
    -4 e2e : MariaDB, core, proxy et agent reels ; agent configure avec le SEUL
       proxy -> connecte au core par le relais, cle du core verifiee, empreinte
       du proxy non ajoutee ; core arrete -> refus immediat ; core relance ->
       reconnexion. Tests unitaires du relais sous -race.
    -5 Documentation : docs/proxy/ (deploiement, relais, securite, prevu,
       depannage).
    -6 Non traite : relais HTTPS et LDAP/S -> TO-DO 72. Releve : TO-DO 73 (le SDK
       journalise « enregistre » meme quand le core refuse).

69.[FAIT-IA] [CLIENT] [DEBUG] Rapport de debug periodique de l'agent
       Demande de Lorens (22/09) : « un mode debug sur les clients qui, toutes les
       minutes (parametrable), ecrit dans un fichier dedie vlt_client-Debug.log un
       rapport propre de l'etat du client : sessions ducky, sessions
       utilisateurs, core/proxy utilise, autres nœuds dans l'ordre, GPO... ».

    -0 Nouveau paquet vaultaire_client/debugreport. Active par
       client_conf.json ("debug": {"enabled", "interval_seconds"}) ou par
       -debug [-debug-interval N] (la ligne de commande prime). Defaut 60 s,
       minimum 5 s. Journal /var/log/vaultaire/vlt_client-Debug.log, rotation
       commune.
    -1 Contenu : agent (versions, PID, chemins), connexion (noeud joint, depuis,
       etablissements, dernier echec, empreinte de la cle en place, empreintes
       de confiance), ordre d'essai avec provenance, sessions Ducky, sessions
       who, comptes provisionnes (uid.map), canal PAM, GPO (etat par scope et
       dernier cycle, modules en echec), revocations, groupes, points
       d'attention.
    -2 Rien ne retenait plusieurs de ces etats : ajoutes sessionmgr.Snapshot
       (SDK, par valeur, sans cle), module.Etat (noeud joint, dernier echec),
       decouverte.DerniereReception, gpo.DerniersCycles.
    -3 Aucun secret (presence de la cle seulement), aucun scan GPO ni appel
       reseau, jamais en mode --fetch-key.
    -4 e2e : agent reel contre un core reel ; rapport ecrit toutes les 5 s,
       noeud joint et sessions justes. Tests : mise en forme, who, non-blocage.

61.[FAIT-IA] [CLIENT] [CLUSTER] Liste des cores ecrite a -join, tenue a jour par l'agent
       Demande de Lorens (point 8 du 21/09, repris le 22/09).

    -0 Core : create -c ... -join depose client_conf.json dans le repertoire
       recopie par SCP : les CORES exposes, en ligne, a empreinte connue, dans
       l'ordre de la 04_04, adresse et port publics (autoaddclientgo.
       EcrireConfClient). rocky.sh le deplace ; sans lui, repli sur l'adresse
       du core qui installe (SSH_CONNECTION). L'adresse fixe 192.168.30.3 a
       disparu.
    -1 Agent : section « learned » de client_conf.json, reecrite (atomique,
       droits conserves) a chaque 04_04 qui change, et configuration en memoire
       mise a jour sous verrou. « servers » n'est jamais reecrite. Seuls les
       noeuds a empreinte de confiance sont persistes
       (decouverte.SurNouvelleListe). Ordre d'essai : memoire, learned, servers.
    -2 config : LoadConfig sous verrou, SaveConfig atomique et qui met la
       memoire a jour (il ne le faisait pas).
    -3 e2e : fichier genere depuis cluster_nodes ; agent redemarre avec un
       « servers » injoignable -> connexion par « learned » sans essayer
       l'adresse morte.
    -4 Releve, non traite : TO-DO 70 (menage quotidien des comptes sans effet)
       et 71 (-join ne sait installer que Rocky).

66.[FAIT-IA] [LDAP] [MFA] Le second facteur au bind LDAP, et son contournement en option
       Demande de Lorens (point 14 du 21/09) : « LDAP doit bypass le MFA si active
       via une option, pas par defaut ».

    -0 L'etat trouve etait l'inverse de la demande
       LDAP contournait le second facteur PAR DEFAUT. Le seul reglage,
       RefuseBindWhenMFARequired, n'etait relie a aucune configuration et, active,
       refusait le bind sans offrir de moyen de le passer.

    -1 Ce qui est fait
       Un compte lie au second facteur (pose par lui, ou impose par un groupe)
       fournit au bind son mot de passe SUIVI du code a 6 chiffres. Le code est
       valide puis consomme (ConsumeMFACounter, meme compteur que le portail et
       08_01 : un code ne sert qu'une fois, toutes portes confondues). Impose mais
       non enrole : refuse. Etat illisible : refuse (le bind laissait passer).
       ldap.mfa_bypass: true (serveur_conf.yaml) laisse ces comptes se lier avec
       le seul mot de passe ; chaque bind concerne part en SECURITY.
       Nexus en mode ldap accole le code saisi au mot de passe.

    -2 Mesure : e2e contre un vrai core (MariaDB). Mot de passe seul refuse,
       mot de passe + code accepte, rejeu refuse, contournement journalise.

    -3 Consequence a annoncer : une application liee avec un compte MFA est
       refusee a la mise a jour. Sortir le compte des groupes MFA, ou poser
       mfa_bypass le temps de la migration.

65.[FAIT-IA] [GPO] dns_resolver plantait sur Rocky 9
       Point 13 du 21/09 : « la GPO dns ne fonctionne pas, le systemctl crash sur
       rocky 9 ».

    -0 Cause : le module ne connaissait que systemd-resolved (drop-in puis
       systemctl restart systemd-resolved). Sur Rocky 9 resolved n'est pas actif :
       le redemarrage echouait (unite absente sur une image minimale), ou
       demarrait un resolveur que personne n'interroge. Le verificateur lisait
       resolvectl, absent lui aussi.
    -1 Le module detecte ce qui tient la resolution : resolved actif -> drop-in
       (inchange) ; NetworkManager actif -> [global-dns] dans conf.d puis
       nmcli general reload ; sinon /etc/resolv.conf (sauvegarde rendue au
       retrait). Aucun service n'est demarre par effet de bord. Echec de
       rechargement -> configuration restauree. Le verificateur lit
       /etc/resolv.conf hors resolved.
    -2 Tests : appliers_dns_test.go (Rocky 9, restauration, resolved prioritaire,
       resolv.conf seul + retrait, verification). Non eprouve sur une vraie Rocky
       9 : voir A_TESTER.md § 11.

64.[FAIT-IA] [CLIENT] [DUCKY] La deconnexion PAM coupait le tunnel machine
       Points 12 et 16 du 21/09 : « si un client perd sa connexion elle doit se
       relancer » ; « un logout propre ferme la session vaultaire principale et
       elle ne se reouvre pas toute seule ; le client reapparait dans status -c
       apres reconnexion ». PamClose.go avait ete modifie pour cibler "vaultaire".

    -0 Trois causes, pas une
       a. PamClose ciblait la session "vaultaire" : le TUNNEL MACHINE. Les
          authentifications PAM passent dans ce tunnel, aucune session n'existe
          au nom de l'utilisateur. La 02_05 envoyee etait en plus mal formee (cle
          de session absente) : le core la rejetait comme usurpation et coupait.
       b. Le --fetch-key de sshd ouvre une connexion vaultaire ephemere a chaque
          connexion SSH. En se fermant (02_05), il effacait la ligne did_login de
          la machine : elle disparaissait de status -c, et le battement 02_12,
          qui ne faisait qu'un UPDATE par cle, ne la recreait jamais.
       c. Le tunnel n'etait ouvert d'office que sur un serveur ; une auth PAM
          pendant une reconnexion lancait une SECONDE boucle ; une panique
          terminait la supervision pour de bon.
       d. Le nettoyage periodique des sessions expirees effacait TOUTES les
          lignes d'un compte des qu'une seule expirait. Tous les tunnels machine
          sont connectes sous « vaultaire » : une machine qui cessait de battre
          faisait disparaitre tout le parc de status -c. Il n'efface plus que
          les lignes expirees.
    -1 Ce qui est fait
       PamClose ne touche plus jamais au tunnel machine. Supervision unique
       (serveur_communication/superviseur.go) : lancee au demarrage sur tout
       noeud, idempotente, relancee apres panique. Core : 02_05 et l'expiration
       n'effacent plus did_login si un autre tunnel authentifie de la machine
       vit ; le battement 02_12 recree la ligne au besoin. Nil guard dans le
       canal PAM quand aucune session n'est disponible.
    -2 Tests unitaires (supervision, relance apres panique, unicite). Non eprouve
       sur un vrai poste : A_TESTER.md § 11.
    -3 Releve, non traite : status -u ne voit aucune session PAM (aucune ligne
       did_login n'est ecrite au nom de l'utilisateur, et il n'existe plus de
       trame de fin de session utilisateur). Reporte au TO-DO sous le 68. La
       formation (jalon 5.3) ne le promet plus.

63.[FAIT-IA] [DOMAINE] Les domaines parents sont crees avec le groupe
       Point 10 du 21/09 : « si infra.acme.lan existe mais pas acme.lan, ca pose
       des problemes ; creer infra.cloud.acme.lan cree acme.lan et cloud.acme.lan ».

    -0 Un domaine n'existe que porte par un groupe (domain_group). CreateGroup
       cree desormais, dans la meme transaction, un groupe du meme nom pour
       chaque ancetre manquant, jusqu'au domaine principal. Le domaine est
       normalise et valide (au moins deux labels). Le dernier groupe d'un domaine
       qui a des sous-domaines ne se supprime plus. Rattrapage au demarrage
       (CreerDomainesParentsManquants), sans effet sur une arborescence complete.
    -1 Nouveau paquet feuille core/domainname (normalisation, validation,
       domaine principal, ancetres). e2e : acme.lan et test.fr crees au
       demarrage d'une base existante ; create -g Web web.cloud.acme2.lan cree
       acme2.lan et cloud.acme2.lan ; delete -g acme.lan refuse.

62.[FAIT-IA] [AUTH] [DOMAINE] On ne se connecte que sous ses domaines principaux
       Points 9 et 11 du 21/09 : « le user peut rentrer n'importe quel domaine ;
       il ne doit pouvoir rentrer que des domaines globaux dans lesquels il est
       via ses groupes (infra.cloud.test.fr = test.fr) ; plusieurs domaines
       possibles, cela fera des comptes differents ».

    -0 Cause : CanUserConnectToDomain ne comparait le domaine tape qu'a
       l'annuaire ENTIER, puis au droit auth. Un compte auth=all se connectait
       sous tout domaine existant. 03_06 (cles pour sshd) ne verifiait pas le
       domaine du tout.
    -1 Le domaine doit etre un domaine principal (deux derniers labels) d'un
       groupe du compte, en minuscules. auth est evalue sur les domaines du
       compte sous ce principal (un suffit). Meme regle sur 03_01, 03_06 et 08_01.
    -2 e2e : alice (Infra infra.acme.lan + Dev dev.test.fr) passe sous acme.lan
       et test.fr, est refusee sous infra.acme.lan, paris.acme.lan,
       vaultaire.fr, nimporte.quoi et Acme.LAN.
    -3 Consequence : les comptes locaux crees sous un sous-domaine
       (user@infra.acme.lan) ne se reconnectent plus sous ce nom.

60.[FAIT-IA] [WEB] Les messages d'erreur s'affichaient en vert
       Point 7 du 21/09. Huit pages ecrivaient leurs erreurs dans Message, rendu
       en alert-success. Elles ont un champ Error rendu en alert-error ;
       alert-danger, alert-info et alert-warning, employees sans regle CSS, en
       ont une.

59.[FAIT-IA] [RBAC] Detacher n'est plus supprimer ; audit matrice / doc
       Points 5 et 6 du 21/09 : « il manque des permissions sur l'interface
       comparee a la doc » ; « group.remove = write:delete:user -> pourquoi j'ai
       le droit de delete un user pour le retirer d'un groupe ? ».

    -0 Audit : aucune cle documentee ne manquait a la matrice. Ce qui manquait,
       c'est un verbe : le modele avait « add » (Rattacher) sans son inverse, et
       chaque retrait empruntait « delete ».
    -1 Nouveau verbe « remove » (Detacher) : group.remove_user/client/
       permission/client_permission/gpo exigent write:remove:<objet>. Colonne
       « Detacher » dans la matrice. Migration unique rbac_verbe_detacher
       (table schema_migrations) : chaque write:delete:<objet> recopie en
       write:remove:<objet>, portee comprise. e2e : 1 droit recopie.
    -2 Signale : write:add:group et write:remove:group ne sont verifiees nulle
       part. write:eyes est affichee obsolete. Doc rattrapee : six actions
       cluster/gpo absentes du tableau des actions, compte de cles de la matrice.

58.[FAIT-IA] [RBAC] [WEB] L'arborescence montrait tout a un delegue
       Point 4 du 21/09 : « eyes -g avec permission restreinte non fonctionnelle,
       pareil sur la page arborescence ».

    -0 La page web (/admin/tree, /admin/api/ldap-tree) exigeait write:eyes et
       lisait la base directement : arbre entier, comptes compris. Elle passe par
       domain.list_tree (read:get:group, filtree) ; les comptes exigent
       read:get:user sur le domaine du groupe ; group-info repond « introuvable »
       hors perimetre. eyes -g <domaine> filtre aussi les sous-domaines (et liste
       des groupes, plus des domaines).
    -1 eyes -g en CLI etait deja filtre dans le code. La CLI LOCALE agit sous
       vaultaire, qui voit tout : a tester avec vlt distant ou le portail.
    -2 e2e : delegue read:get:group 0:infra.acme.lan -> arbre reduit a Infra,
       sans comptes ; group-info Paris -> 404.

57.[FAIT-IA] [WEB] [RBAC] La page Cluster s'ouvrait sans read:cluster
       Point 3 du 21/09. AdminClusterHandler exigeait encore read:get:client.
       Corrige, et ajoute a web_cles_des_pages_test.go. e2e : refus pour un
       delegue sans read:cluster.

56.[FAIT-IA] [DOC] [TRAINING] Jalon 1.3 : le mot de passe de vaultaire
       Point 2 du 21/09. vaultaire/password n'a jamais fonctionne : l'empreinte
       semee est un MD5 que le core ne lit pas. Le jalon se connecte avec admin,
       DONNE un mot de passe a vaultaire (compte de secours : portail seul, pas
       de LDAP ni SSH, sans expiration), et dit comment retirer la cle de demo
       d'admin (modifier la section administreur apres le premier demarrage n'a
       aucun effet). README racine corrige. e2e : vaultaire/password refuse,
       mot de passe pose accepte.

55.[FAIT-IA] [DOC] [TRAINING] La mise en place de vlt
       Point 1 du 21/09. Bloquant : le certificat de l'API est produit avec les
       noms du CONTENEUR ; vaultaire_ctl verifie le nom -> x509 a coup sur. Le
       jalon 3.2 fait regenerer le certificat api avec l'IP de l'hote puis
       redemarrer. Aussi : alias en -i (et non -it), conflit alias/binaire vlt,
       binaire deja present dans cmd/vaultaire_ctl sur l'hote, horloge, sortie
       « Resultat: ». Aide de certificate show completee (web, api).
       vaultairectl.md : libelle de cle manquant a add -u -k.

54.[FAIT-IA] [NEXUS] [DUCKY] [RBAC] Vaultaire Nexus et la categorie 08
       Iteration precedente, jamais consignee ici. Type de client
       vaultaire_nexus, cles read:nexus / write:nexus / write:nexus_admin,
       attribut LDAP vaultaireServiceRights, categorie 08 generique
       (authentification par un service, TOTP compris), mode auth ducky de
       Nexus, protocole Ducky decoupe en ducky-network/, guide Nouveau_service.md.
       Carte detaillee : src/vaultaire_nexus/CORE_CHANGEMENTS.md.

# ─── Début du cycle 2.2 (21/09/2026) — les entrées 54 à 66 viennent du cycle 2.1, non publiées en v2.1.0 ───
